rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users (teachers) can read/write student data
    match /students/{studentId} {
      allow read, write: if request.auth != null;
    }
  }
}
